# 测试 Markdown 渲染

这是一个**测试文档**，用于验证新的 Markdown 渲染器功能。

## 支持的功能

### 1. 文本格式化

- **粗体文本**
- *斜体文本*
- `内联代码`
- ~~删除线~~（如果支持）

### 2. 列表

#### 无序列表
- 项目一
- 项目二
  - 嵌套项目 A
  - 嵌套项目 B
- 项目三

#### 有序列表
1. 第一步
2. 第二步
3. 第三步

### 3. 代码块

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
  return `Welcome, ${name}`;
}

// 测试函数调用
greet("World");
```

```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

# 计算第10个斐波那契数
result = fibonacci(10)
print(f"Fibonacci(10) = {result}")
```

### 4. 引用

> 这是一段引用文字。
>
> 引用可以包含多行内容，并且支持**格式化**和`内联代码`。

### 5. 链接

[访问 GitHub](https://github.com)

### 6. 表格

| 功能 | 状态 | 描述 |
|------|------|------|
| 标题 | ✅ | H1-H6 全部支持 |
| 列表 | ✅ | 有序和无序列表 |
| 代码 | ✅ | 带语法高亮 |
| 引用 | ✅ | 嵌套引用 |
| 链接 | ✅ | 自动安全处理 |
| 表格 | ✅ | 响应式设计 |

### 7. 分隔线

---

## 高级测试

### 嵌套列表

1. 主项目一
   - 子项目 A
   - 子项目 B
     - 更深层级
   - 子项目 C
2. 主项目二
   - 另一个子项目

### 代码和文本混合

在段落中插入`console.log('Hello')`这样的代码片段，以及**粗体**和*斜体*的组合使用。

### 多级引用

> 外层引用
>
> > 内层引用
> >
> > > 更深层引用
> >
> > 回到内层
>
> 回到外层

---

## 总结

这个测试文档涵盖了 Markdown 的主要语法元素，新的渲染器应该能够正确处理所有这些元素。